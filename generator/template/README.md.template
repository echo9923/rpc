# {{SERVICE_NAME}} Generated Project

This directory was generated by `generator/tinyrpc_generator.py` from `{{PROTO_FILE}}`.

Layout: `{{LAYOUT_NAME}}`.

{{README_LAYOUT_NOTE}}

## Build

```bash
cmake -S . -B build -DMYTINYRPC_ROOT=/path/to/rpc
cmake --build build
```

## Run

```bash
bash run.sh
./{{CLIENT_BIN_REL}} --client {{SERVER_PORT}}
bash shutdown.sh
```

`run.sh` starts `{{SERVICE_NAME}}_server` with `{{CONFIG_FILE_REL}}`, writes a pid file, and waits until the generated client can probe the TCP port. `shutdown.sh` sends a normal termination signal to that pid; the generated server catches it and calls `tinyrpc::StopRpcServer()` before exiting.

## Current Scope

- The generated service implementation delegates each method to one interface class.
- Protobuf C++ files are generated by `protoc` when the project is generated.
- The generated client calls each unary rpc method with default request values through `TinyPbRpcChannel::Ptr` and Channel-level connection reuse.
- The generated CMake project depends on the local MyTinyRPC source tree passed by `MYTINYRPC_ROOT`.
