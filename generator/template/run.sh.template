#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "[{{SERVICE_NAME}}] generated project root: ${ROOT_DIR}"
echo "[{{SERVICE_NAME}}] run placeholder"
